- content_for :content_table do
  #general_table

  = yield

  :javascript
    var project = #{project ? project.serializable_hash.to_json : 'nil'};
    var title = "#{title}";
    var data = #{table_data};
    var model_name = "#{hash_model_name}";
    var model_path = "#{model_path}";
    var current_user = #{current_user.to_json};

= render file: 'layouts/application'


