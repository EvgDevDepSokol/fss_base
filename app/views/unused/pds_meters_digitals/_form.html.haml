= form_for @pds_meters_digital do |f|
  - if @pds_meters_digital.errors.any?
    #error_explanation
      %h2= "#{pluralize(@pds_meters_digital.errors.count, "error")} prohibited this pds_meters_digital from being saved:"
      %ul
        - @pds_meters_digital.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :IC
    = f.number_field :IC
  .field
    = f.label :sys
    = f.number_field :sys
  .field
    = f.label :Project
    = f.number_field :Project
  .field
    = f.label :ctrl_power
    = f.number_field :ctrl_power
  .field
    = f.label :t
    = f.datetime_select :t
  .actions
    = f.submit 'Save'
